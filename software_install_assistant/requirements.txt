# 后端服务自动诊断修复 Skill 依赖
# Auto Fix Skill Dependencies

# 核心依赖
pyyaml>=6.0

# OCR 图片识别依赖
Pillow>=9.0.0           # 图像处理
pytesseract>=0.3.10     # Tesseract OCR 包装器

# 可选依赖（用于增强功能）
# aiohttp>=3.8.0  # HTTP 请求支持
# tenacity>=8.0  # 重试机制

# Windows 特定
# pywin32>=300  # Windows API 支持（用于服务管理）

# 文档/测试
# pytest>=7.0
# pytest-asyncio>=0.20
